#include<iostream>
#include<string>
#include <fstream>
#include <sstream>
#include <vector>
using std::string;
class librarian
{
	private:

	string bookname;
	string author;
	string bookno;
	string num;
	
	public:

	librarian();
	void addBook();
	void updateBooks(string name,int copies);
	void deleteBooks();
	int countLeapYears(date d);
	int getDifference(date dt1, date dt2) 
	int calc
	
};
